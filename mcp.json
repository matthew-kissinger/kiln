{
  "mcpServers": {
    "kiln": {
      "type": "stdio",
      "command": "bun",
      "args": ["run", "${PLUGIN_ROOT}/src/mcp-server.ts"],
      "env": {
        "KILN_RENDER": "auto"
      }
    }
  }
}
