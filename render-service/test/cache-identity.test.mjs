import {test} from 'node:test';import assert from 'node:assert/strict';import {mkdtempSync,writeFileSync,rmSync} from 'node:fs';import {tmpdir} from 'node:os';import {join,resolve,sep} from 'node:path';import {fingerprintRendererInputs} from '../src/cache-identity.mjs';
test('renderer fingerprint changes with source, installed runtime bytes, and adapter',()=>{const dir=mkdtempSync(join(tmpdir(),'kiln-render-id-'));try{writeFileSync(join(dir,'renderer.mjs'),'one');const state={rendererId:'gpu',backend:'d3d12',summary:{device:'one'}};const first=fingerprintRendererInputs([dir],state);assert.equal(first,fingerprintRendererInputs([dir],state));writeFileSync(join(dir,'renderer.mjs'),'two');assert.notEqual(first,fingerprintRendererInputs([dir],state));assert.notEqual(fingerprintRendererInputs([dir],state),fingerprintRendererInputs([dir],{...state,summary:{device:'two'}}));}finally{assert.ok(resolve(dir).startsWith(resolve(tmpdir())+sep));rmSync(dir,{recursive:true,force:true});}});
