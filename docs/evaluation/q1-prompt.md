Read this project's AGENTS.md and installed Kiln skills. Use the attached asset
brief to build a complete asset in this workspace. Discover the helpers you need
through Kiln. Do not read the engine repository, gallery examples, other model
runs or files outside this project.

Give the asset clear named parts. Render it and review the images. Use useful
whole-asset views and a close view of `ServiceAssembly`, including its attachment.
Check the returned camera and material fidelity before drawing conclusions. Use
custom geometry where the shape needs it; do not add complexity solely to use a
helper. Keep the original revision reference.

Once you have a first version, find `serviceOffset` with a bounded source read.
Increase it by 0.15 metres using `kiln_edit` with the saved reference. Do not send
the whole program again or rewrite unrelated source. Review the revised asset
with the same whole-asset camera and a useful part-relative detail camera. Inspect
the translation animation at the beginning, middle and end to verify attachments.

Export the final `.kiln.js` and `.glb` into `assets/`. Report the original and final
revision references, output paths, what changed, and any remaining limitation.
Do not claim a check succeeded unless the tool output or image supports it.
