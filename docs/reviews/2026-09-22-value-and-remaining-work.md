# Kiln value review and remaining atomic work

The later [v0.8 candidate pass](2026-09-23-v08-candidate.md) now records current
installed qualification, fresh dogfood, and atomic remaining gates. The closeout
and acceptance mapping below retain their original historical scope.

The current checkout closeout is complete; [the handoff](2026-09-23-checkout-handoff.md)
contains qualified identities, evidence and limitations. All original requirements
remain in the [136-task ledger](../plans/2026-09-21-implementation-ledger.json).

> Finish Kiln’s unified checkout by resolving remaining shared defects, reconciling completed validation and finding dispositions, and delivering accurate support documentation and an exact-candidate handoff. Preserve explicit package/install/release gates. Reuse existing evidence and reopen implementation only for a demonstrated checkout blocker.

## Completed checkout value

Category-free requirements and offline Discovery now serve shared CLI/MCP authoring.
Geometry/assembly fixes, repair diagnostics, surface/part/animation inspection and
local/remote rendering have focused and integrated evidence. Optional Strands uses
the shared contracts with its own native finish/context, isolated from external skills.

The campaign reviewed 36 main authorings and 72 edits, plus 12 held-outs and 24 edits.
Native13 completed generation and both refinements. All 255 findings and ten audit
gaps have explicit dispositions; original asset defects and route failures remain.
The original runtime coverage gate passed 2,642 tests with two skips and zero
failures: 95.16% functions / 92.21% lines above unchanged ratchets. The subsequent
[blind onboarding and game follow-up](2026-09-23-blind-game-followup.md) corrected
OpenCode setup guidance and false workspace drift between installed Node
interpreters. Its full test gate passed 2,643 tests with two skips and zero failures;
coverage was not rerun. The engine/rendering source and five runtime bundles retain
their qualified identity. The setup fix and documentation are a recorded delta
from the original closeout manifest.

Ledger states: **115 implemented, 8 complete, 1 research evidence-ready, 11 in progress, 1 pending**. Different-sized implementation,
research and acceptance rows are not a completion percentage. Eight completed rows
include research decisions and evidence reconciliation, not eight new product features.

## Stop conditions and value judgment

Do not repeat completed campaigns or repair scored/gallery assets to improve the
appearance of the result. Model quality and inspection completeness remain imperfect.
Broad fixes earned their place through reusable failures; another boat/roof/bracket
factory has no demonstrated benefit. General shell/bevel/remesh and frame-attachment
candidates have explicit measured decisions, not silent deferrals.

Further provider surveys, original-audit repeats, unrelated preflights and status
bookkeeping are not required. The final finding reconciliation, support corrections
and candidate handoff close this checkout pass. Reopen code for a reproduced shared
blocker; otherwise the next substantial work is the separately authorized installed
package and release phase.

## Remaining atomic gates

### Before a v0.8 source checkpoint

This is a readiness recommendation, not version, commit, push or deployment
authorization. Package and plugin versions are currently 0.7.1.

1. Bring the release-facing changelog up to the unified workflow. Its leading
   Unreleased section still describes owner-exit renderer shutdown and the retired
   native `kiln_screenshot` split. Current rendering/tool/runtime guides describe
   managed idle shutdown and the shared program-reference workflow. State the
   breaking changes and link the existing migration guide; preserve dated evidence
   as history rather than rewriting it as current acceptance.
2. If the owner chooses 0.8.0, update the package and three plugin manifests
   together, rebuild the distributed runtime, and verify the resulting candidate.
   Existing tests qualify the current checkout; a changed version/build needs its
   own relevant identity and consistency checks.
3. Review the intended commit contents, including new files and generated bundles.
   The working tree contains substantial implementation, research and evidence;
   the historical manifest is not an instruction to stage everything. Check the
   final diff and public contents before committing.
4. Choose the push/deployment scope explicitly. CI builds a test tarball without
   publishing. The Pages workflow deploys qualifying pushes to `main`, including
   source or README changes; a source push there can also update the gallery.

Shared skills and current setup/Discovery/native-boundary guidance were reviewed;
`check:skills` passes for six skills and two byte-identical registered copies. That
check covers format and copy consistency, not every instruction's effectiveness.
Two new dogfood findings remain proposals, not completed tool/skill upgrades:

- Generic loop-closure evidence: the creature's 21 tentacle rotation tracks had
  endpoint gaps up to 9.7819 degrees despite a seamless-loop claim. Distinguish
  intended one-shot motion and equivalent quaternion signs before adding a check.
- Preservation evidence: the cave edit changed all eight mesh geometries while
  claiming unrelated geometry was unchanged. The agent skipped the comparison
  already required by the shared refinement skill. Qualify a better completion
  workflow before claiming the existing guidance solves this failure.

These are worthwhile general improvements, not reasons to repair gallery assets
or repeat the broad campaign. They need an explicit disposition for a v0.8
checkpoint and should not be represented as fixed. Existing Discovery ranking,
QA coverage, Cline image handling and hardware/provider limits remain documented
in the [handoff](2026-09-23-checkout-handoff.md).

### Before package/install/release acceptance

| Task | Remaining acceptance after this handoff |
| --- | --- |
| D18 | Low-power and cold installed offline Discovery; retain unsupported-query limits. |
| S09 | Broader live provider/route qualification before advertising it beyond offline contract support. |
| L07 | Negative retired-path checks on the final installed package. |
| B02 | Complete native notice inventory and verify release provenance; research supports the chosen dependency strategy. |
| H03 | Full native dependency/platform installation matrix, optional omission, disabled scripts, read-only and failure cases. |
| H04 | Cold official package resolves renderer source/hooks/native dependencies without a nested manual install. |
| H11 | Cold installed CLI/MCP/native-to-PBR flows and realistic platform/lifecycle/performance qualification. |
| V03 | Preserve the original installed-package campaign clause; completed checkout runs are not silently relabeled. |
| V06 | Full package/workspace gates when packaging is authorized; checkout gates already pass. |
| V08 | Package digest and later commit reconciliation; current tree/bundle evidence is delivered here. |
| V10 | Preserve final installed-candidate acceptance separately from the completed frozen checkout campaign. |
| N03 | Actual cold installed minimum/newer Node and Debian/Cline boundaries; Windows/CachyOS checkout results are distinct. |
| N04 | Complete Debian/classroom guidance after those actual installed checks; supplied unnamed community feedback is already credited. |

These 13 rows retain their original acceptance clauses. No pending package test is
declared passed and no release action is authorized. The precommit manifest is an
observed checkout snapshot, not a request to stage all existing user work.
