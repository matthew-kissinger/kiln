# Checkout cutover acceptance

This review closes concrete API and guidance gaps found while reconciling the
completed main campaign. It does not qualify an installed package, native model
completion or final held-out acceptance.

## Corrections

- **F249, migration review:** generic category warnings did not name known policy
  differences or the separate scope/modular trigger. Reports now describe the
  retained measurements, modes and limitations, including the 8 cm historical
  container assumption, scene-dependent circular advice, changed rig selection,
  inferred scope and unavailable modular-join coverage. No rule is enabled,
  weakened or declared equivalent. Four focused tests failed before the change;
  the corrected five-file migration suite passes 62 tests. Eight actual compiled
  Node CLI conversions, covering all seven old categories and explicit modular
  scope, return review-required and leave their input bytes unchanged.
- **F250, external guidance:** `site/public/llms.txt` still instructed agents to
  invoke the removed primitive-list tool. It now names Discovery, distinguishes
  native Strands context from shared skills, and labels gallery assets as unvetted
  history. No published site was changed. Existing site-resource and skill-reference
  checks pass five tests, including archive byte/digest parity and executable
  reference source. Six shared skills and both registered setup copies pass.
- **F251, public QA:** `@kiln/engine/qa` still exported six old aggregate entry
  points and omitted the neutral requirements pipeline. The current runner,
  evidence collection, final-artifact checks and report types are now public.
  Old aggregate exports have no aliases. Historical implementations remain
  internal for comparison fixtures; shared low-level measurements and old record
  types remain available. Two tests reproduced the gap, then passed through the
  actual package self-reference. Eleven focused QA checks pass. Migration guidance
  names the new pipeline and distinguishes scene checks from final-GLB acceptance.

## Task dispositions

P01/L03 are implemented for explicit conversion and reviewed policy replacement.
Their acceptance requires originals, a new contract and diff/report, visible stops
for unmappable obligations, and no normal legacy execution branch. It does not
require silently reproducing every category assumption. Automatic equivalent
conversion remains unavailable; unresolved obligations require review and a
documented host decision. The earlier immutable rebuild and subsequent MCP edit
proof remains valid; this change only expands the review record.

L01's inventory now includes the public QA aggregate and the settled renderer
decision. L06's current guidance scan found and corrected the site instruction;
other retained old names describe migration/history. The obsolete `kiln-glb`
skip task was stale: no such skips remain. The two suite skips on Windows concern
POSIX file permissions. Historical gallery execution/provenance checks remain;
gallery repair and quality endorsement remain outside scope.

D12's checkout parity is evidenced by generated tool-reference validation, current
skill/reference checks, deterministic site-resource byte checks and the retained
58-file fresh-workspace native-isolation receipt. Native workflow instructions
remain programmatically registered outside `skills/`. Installed-package parity
and live site publication remain later gates.

L08 also has a bounded checkout disposition. The retained real Coastal Proving
Ground migration reads original GLB/source bytes unchanged, explicitly rebuilds
under reviewed current requirements, then restores, reads, edits and saves through
standalone MCP. Its 971 nodes, 142 meshes, materials, images and animation binary
are unchanged by the unedited rebuild. The recorded default-prop intent recovery
is an explicit assumption, not reconstructed historical intent. This is a migration
fixture, not a gallery-quality endorsement.

Current rebuild tests additionally cover failed authority/rebuilds leaving only
the original revision, explicit source repair, incomplete QA, missing intent and
policy snapshots. The workspace tests exercise actual CLI/MCP startup rejection
on stale skills, followed by explicit upgrade and restored Discovery; conflicting
local edits stop before mutation. Those five workspace checks were rerun after
the final setup-guidance change. Together with the seven-category/scope conversion
checks above, this covers L08's checkout requirements. Installed migration and
platform qualification remain later; universal legacy-policy equivalence is not
implemented or claimed.

## Fresh Discovery observations

Sixteen queries were written before execution: twelve supported construction
requests and four unsupported requests. The author had seen catalog contracts;
this is not independent or blind evaluation. No metadata or ranking was tuned
after seeing results. The current catalog contains 129 entries.

All twelve supported requests retrieve an acceptable operation/recipe in the
first five results; mean reciprocal rank is 0.753. The results include recent
joined-frame, editable-assembly, surface-detail, foliage-cutout and directional
sweep recipes. Several first results are less useful than lower-ranked results.
All four unsupported requests return lexical candidates, sometimes unrelated.
Discovery is candidate retrieval, not a semantic capability or support verdict.

The same sixteen queries produce identical rankings through actual compiled Node
22 CLI calls, in a clean directory with no provider keys, CPU mode, and a preload
that rejects network and child-process calls. Cold CLI duration was 488–499 ms;
maximum response size was 7,248 bytes. Bun index creation was 17.2 ms with a
9.7 MB observed RSS increase; maximum warm p95 was 0.79 ms. These are single
desktop observations. Index timing excludes imports, RSS deltas are not peak
memory, and the preload is not an OS network sandbox. Existing baseline/growth
measurements remain historical; low-power and installed offline checks remain D18.

## Evidence

External evidence under `C:/Users/Mattm/X/kiln-dogfood/unification-2026-09-22/`:

- `migration-policy-review-final/receipt.json`, `migration-policy-focused.log`
- `skills-parity-review.log`, `native-skill-boundary-review/receipt.json`
- `qa-public-surface-focused.log`, `qa-public-surface-final-gate.log`
- `discovery-fresh-judgments.json`, `discovery-fresh-results-bun.json`
- `discovery-offline-review/receipt.json`

The initial migration probe used the wrong report envelope, and the initial offline
probe omitted CLI `--query`; both were driver errors. Corrected probes use the
documented interfaces without product workarounds. The progress checkpoint owns
the latest completed full gate and exact runtime identity.

## QA acceptance before held-outs

Q02's requested calibration cases are covered by existing reciprocal fixtures and
actual interfaces, not by gallery quality. Open sheets and intentionally floating
parts remain exportable; missing solid-volume coverage is explicitly unmeasured.
Requested grounding uses real vertices, including instances and transformed roots,
with the retained advisory tolerance. Valid static mechanisms remain incomplete
for motion/usable-opening certification, while declared obstructed hinges and
openings produce blockers. A slider clearance cannot satisfy a same-named hinge.
The two six-case CLI/MCP receipts reproduce accepted, blocked and incomplete
outcomes, including an unrequested mechanism, a 2 cm opening, a floating six-level
structure, a missing requested storey and unavailable modular/semantic checks.

This completes measurement of the specified cases, not universal false-positive
or false-negative rates. Main-campaign misses remain visible: uninspected individual
joints, neighboring geometry obstructing an opening, and unsupported robot gait.
Structural `accepted` is not a claim that the brief, all attachments or animation
quality passed. The preserved original authorings remain evidence of those limits.

Q03's interface consistency is covered by the current full gate and independent
transport receipts. `cli-render-json.test.ts` checks the exported GLB hash,
retained reference, policy, part listing and CPU image against the shared tool
result. Registry tests preserve binding snapshots across asynchronous evaluation.
Derivative-view tests bind animation/interior receipts to one artifact and reject
unacknowledged cameras; current fallback failures remain visible. Capture-cache
tests exclude unverified material/camera receipts and keep CPU/GPU entries separate.
Actual GPU receipts in the main campaign identify reviewed GLBs; native adapter
tests preserve typed image blocks. CPU `materialFaithful: false` never becomes
material evidence. Provider image delivery and live native completion remain
separate acceptance questions, not claims established by these offline adapters.

These are bounded implementation dispositions for Q02/Q03. The final held-outs,
remaining native trial and installed/platform gates are still required separately.
Relevant receipts: `neutral-domain-acceptance-review/receipt.json`,
`neutral-mechanisms-standalone/receipt.json`, `placement-bounds-review/receipt.json`
and `revision-comparison-final-review/receipt.json` under the external evidence root.

## Renderer setup guidance: H10

Current README, setup skill and generated workspace instructions now agree on
the integrated optional renderer, lazy local startup and explicit remote routing.
They no longer require a session restart just to recover from repaired renderer
dependencies or a cached startup failure: `kiln_renderer({action:"reprobe"})`
refreshes the existing MCP route. CLI `service reprobe` affects only its own
process. Runtime, environment and credential changes still require a restart.

Remote setup names the MCP process environment (`KILN_RENDER_PORT_URL` and
`KILN_RENDER_TOKEN`) and CLI `--render-port`. The remote device runs and manages
its compatible service, with explicit authentication for a wider bind. Readiness
is kept separate from an actual material-faithful render receipt.

Seven fresh checkout workspaces, one for each supported setup harness, pass
creation and `--check` under Node 22.23.2. Their generated Markdown contains no
native workflow or finish instructions. Actual compiled CLI and service help
agree with the documented options and lifetime. Thirteen focused workspace/
harness tests and the skill-format/registered-copy checks pass.

Receipt: `renderer-guidance-review/receipt.json` under the external evidence root.
This closes H10's documentation requirement at checkout scope. Existing H09
cross-device checkout proof remains valid; this review adds no GPU or remote
operation evidence. Cold installed renderer and supported-platform qualification
remain under H03/H04/H11; no package was created or installed for this review.
