# Frozen checkout held-out campaign

Twelve authorings use six previously frozen briefs twice, with two prescribed
edits per authoring. Runtime `ff686c2086d063660c8b4cb68d79d519a83e90e8d30132d31234106aaccdbfbf`
and the workspace guidance stay unchanged between trials. OpenCode 2.0.14 uses
`opencode-go/deepseek-v4.1-flash`. Completion counts require all three stages and
independent review; they do not certify asset quality.

Evidence root: `C:/Users/Mattm/X/kiln-dogfood/unification-2026-09-22`.
`heldout-briefs.json` and `heldout-freeze.json` retain the exact briefs, hash and
candidate. Original sources, images and traces are not repaired by the reviewer.
Baseline limit is 45 minutes; each edit has 15 minutes. An infrastructure retry
must retain the original attempt and its reason; no model substitution is allowed.

All **12 authorings and 24 edits are independently reviewed**. This closes the
checkout campaign, with the quality failures below retained. It does not qualify
an installed package or establish that every delivered asset satisfies its brief.

## Heldout01: articulated task lamp

Baseline completed in 739.395 seconds, exit 0. Exact source, saved GLB and exported
GPU image match their recorded hashes. The image was read through the harness.
There are 34 meshes, three Fold animation channels and a real editable joint
hierarchy. The sampled animation shows attached arm assemblies and a level head;
it is not continuous collision certification.

The trace contains 12 Discovery calls. Two invented exact helper IDs receive an
explicit error and useful suggestions; the model recovers. An inspection using
bare names instead of exact paths also fails explicitly and is corrected. The
model measures eight selected interfaces, but its claims about credible pivot
hardware omit the caps and washers.

Independent frozen-CLI checks find all six cap-to-axle gaps at 1.500 mm, cap-to-
washer gaps at 2.810 mm, washer-to-axle separation at 0.988 mm and washer-to-arm
gaps at 0.500 mm. The diffuser is 2.715 mm from the shade and 54 mm from the cap
and neck, with no support modeled. Its recorded shell separation is described
by the author as an inset rather than an unresolved attachment. The close-ups
make these construction weaknesses visible.

These are selected tessellated-surface measurements, not a universal contact
test. Positive distance alone can indicate containment, and other reported gaps
between a boss and bars are bridged by an axle. The verified weakness is missing
interface coverage and interpretation, extending F219; the shared tool correctly
reports the selected distances. No global contact tolerance or lamp-specific
restriction follows from it. Keep it distinct from the successful artifact and
image transport checks.

Evidence: `evidence-heldout-01-task-lamp-mcp-deepseek/` contains
`independent-artifact-review-stage-0.json`, `fit-review-stage-0.json`, original
CLI receipts, `stage-0-trace-review.json` and the original author trace. The first edit completed in 773.165 seconds, exit 0. It changes lower link length
from 0.28 m to 0.35 m, preserves the 0.22 m upper link, all ten base meshes,
materials and every Fold channel, and changes only the two lower bars' geometry.
Its trace contains seven inspections and no retained domain failures. The same
hardware/diffuser gaps remain, as expected from the protected edit scope. Source,
GLB, saved child lineage and GPU image-read checks pass. The second edit completed in 597.095 seconds, exit 0, and final qualification
passes. It replaces the conical head with a rounded rectangular housing, bezel
and diffuser under one new assembly root. All three Fold channels, the head
pivot and 31 of the 32 meshes outside the original shell/diffuser remain exact,
including material bindings. It also removes `Mesh_Head_Cap`, the old circular
closure beneath the neck: this exceeds the narrowest literal replacement set,
though it leaves every pivot cap and the attachment axis intact. Keep that scope
qualification visible rather than calling the entire replacement unchanged.

The new housing meets the neck and bezel; the diffuser meets the housing. Its
1.202 mm lateral separation from the bezel is bridged by that backing housing.
The six original 1.500 mm cap/axle and 0.500 mm washer/bar gaps persist in all
three revisions. Other contact/containment or continuous-motion claims remain
unqualified.

The original edit CLI commands pipe one-line JSON through PowerShell
`Select-String`; display highlighting/wrapping then consumes the harness's output
limit. The complete retained `viewFidelity` objects are recoverable after removing
that formatting and bind each actual GPU render to the independently verified
saved/exported GLB hash. The surrounding receipts are truncated; no omitted
fields are inferred. Native reads of the exact PNGs and immutable parent revision
lineage are verified separately. This is an author command/evidence limitation,
not evidence that the CLI failed to emit structured JSON.

The three stages contain 33/10/11 started model steps and 24/15/13 actual MCP
calls. Nominal harness cost totals $0.366598698; this is not a measured wallet
debit. Session export verifies all three exact prompts, one provider/model route
and unchanged runtime/guidance. `trial-qualification.json`,
`geometry-review-final.json`, three `fit-review-stage-*.json` files and original
traces retain the evidence. Heldout01 counts as one reviewed authoring and two
edits, with the above quality and evidence limits.

The first edit had a long interval without emitted CLI events, then completed
normally. This is not a recorded timeout, provider failure or infrastructure retry;
no causal claim is made from the quiet interval.

## Heldout02: articulated canal gate

All three authoring processes completed: 571.049 / 232.172 / 267.019 seconds,
with 32/14/10 started model steps and 29/11/7 MCP calls. Nominal harness cost is
$0.31824993, not a measured wallet debit. Exact prompts, route, frozen runtime,
saved child lineage, final GPU receipts and native PNG reads are verified.

The baseline had three opaque material execution rejections. It recovered using
approved textures and `pbrMaterial`; the [shared contract correction](2026-09-23-material-contract-feedback.md)
is separately qualified on the current checkout. Held-out sources and runtime
remain unchanged. The rendered materials are real textured GPU outputs; the stone
reads light cream and unusually coarse. This is not an aesthetic endorsement.

The first edit widens the clear opening from 3 to 3.6 m. Abutment and hinge
geometry stay exact, gate height stays 1.7 m, deck width stays 0.5 m, and materials,
textures and Open channels are unchanged. The original 20 mm center gap remains;
the literal request that the meeting edges meet is therefore not fully satisfied.

The second edit adds a bowed left rail and closely spaced balusters. The left
leaf and deck stay exact, with the rail parented to the moving joint. All 32 right
assembly nodes retain geometry, transforms and material bindings. Ten right-rail
mesh names change because the authored global duplicate-name pass renumbers them
when the left rails disappear. Geometry preservation does not imply identity
preservation. A preliminary reviewer hash alert was only accessor-key ordering;
the qualified comparison canonicalizes keys and confirms equal accessor values.

The model reports possible deck/masonry interference at full Open and correctly
limits its claim to overlapping bounds. Independent exported-geometry checks
confirm actual volume overlap on both sides in all three revisions: selected
points lie strictly inside both the box deck and box abutment after applying the
exported animation. At full Open, witnesses are approximately 186 mm inside the
bank at baseline and 184 mm after widening. These are witness depths, not maximum
penetration estimates. This checks selected poses and parts, not continuous motion.
Original hinge/strap fit limitations also remain; the baseline is not a golden asset.

The final CLI receipt was deliberately saved to `stage-2-receipt.json`; shell
output only said `exit=0`. The controller stopped at its evidence reader, not
the authoring process. Review now reads that exact observed file, retains its hash
and checks the GLB/fidelity binding. No authoring was repeated. Earlier truncated
PowerShell output retains the complete required fidelity object, as in Heldout01.

Evidence: `evidence-heldout-02-canal-gate-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review-qualified.json`, three trace reviews,
artifact comparisons and original events. Heldout02 counts as reviewed completion
with two edits, with the unresolved quality and identity findings above. The
next sequential controller starts trial3; it never silently retries a failed stage.

## Heldout03: folding luggage cart

The baseline and two edits complete in 465.826 / 131.367 / 175.683 seconds,
with 27/10/10 started model steps and 26/8/7 MCP calls. Nominal harness cost is
$0.231542076. Exact prompts, route, frozen runtime, saved lineage and image reads
are verified. One invented exact `createGroup` Discovery ID is rejected with
suggestions; the model recovers. There are no retained edit-stage domain failures.

Wheel-center spacing grows exactly from 0.43 to 0.5375 m. Only axle geometry and
the eight wheel-mesh world positions change. Wheel geometry, frame, grip, platform,
materials and Fold channels are preserved. The model discloses the longer exposed
axle between frame-mounted brackets and hubs instead of describing it as unchanged
support geometry or certifying mechanical strength.

The curved grip replacement preserves all 24 protected meshes, transforms and
material bindings, and every Fold channel. It retains a replaceable Grip root.
Both collars move 4 mm inward; the model preserves the former grip centerline
endpoints, not the old collar transforms. Keep that distinction when judging
the request to preserve attachment points.

The baseline's straight sleeve exposes the curved frame above it. Independent
exported-geometry rays find 7.545 mm of exposure at the center and 5.072 mm at
z = +/-0.05 m. The requested curved replacement covers the frame at all five
measured vertical sections. This is a real improvement using existing helpers,
not whole-sleeve fit certification. The frame's spline overshoot remains, and the
model explicitly reports it after refinement. There is no evidence here that a
new grip-specific helper or a category restriction is needed.

Edit-stage CLI commands select fields with PowerShell `Format-List`, dropping
fidelity metadata while retaining successful required-GPU status and exact GLB
hashes. Independent maintainer replay of the unchanged sources on the frozen
runtime produces byte-identical original GLBs **and PNGs**, with full faithful GPU
receipts. Those receipts belong to the reviewer replay, not the original model
events. No model run was repeated and no scored source or image was replaced.

Evidence: `evidence-heldout-03-luggage-cart-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review.json`, `review-gpu-stage-1-proof.json`,
`review-gpu-stage-2-proof.json`, the original three-stage traces and comparisons.
Heldout03 counts as reviewed completion with two edits and the above limitations.
The sequential campaign resumes at trial4 on the same frozen candidate.

## Heldout04: flowering branch

The three stages complete in 347.516 / 192.829 / 211.741 seconds, with 26/12/11
started model steps and 28/9/9 MCP calls. Nominal harness cost is $0.09323019.
Exact original GPU receipts, image reads, frozen runtime and immutable lineage
pass. No reviewer render replay or source correction was required.

The baseline recovers from an invented exact curve-helper ID and an invalid
camera subject. One failed render is reduced by the author's own tool wrapper
to `ok:false` and warnings: it omitted the error field. The trace does not retain
that failure's full diagnostic, so it is not attributed to Kiln losing an error.
The subsequent missing-subject error gives exact paths and the model recovers.

Independent exported vertices confirm all six upper petals widen along local X
by 20%, within 9.4e-10 m Float32 rounding. Every Y/Z coordinate and mesh transform
is unchanged. All 37 protected meshes and their material bindings remain exact.
The middle-leaf replacement changes only blade and vein geometry; all 41 protected
meshes, the leaf attachment root and materials remain exact. Its three-lobed form
is visibly spiky, as disclosed by the model, and retains its own editable root.

Independent frozen-CLI checks confirm selected petiole/stem and petiole/blade
surface contact. The new vein has a minimum surface gap of 0.162811 mm, matching
the author's disclosed limitation after its own refinement. Minimum distance
does not certify whole-surface conformity or complete overlap at every petal root.
Thin leaf/petal sheets are intentionally open and retain incomplete solid-volume
coverage. No texture is claimed; all materials use scalar colors. The studio's
lighter appearance alone does not establish an export or renderer defect: the
exported linear base-color factors match the authored colors.

Evidence: `evidence-heldout-04-flower-branch-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review.json`, the paginated part listing,
selected CLI fit receipt and original three-stage traces. Heldout04 counts as
reviewed completion with two edits and the above limits.


## Heldout05: two-tier aqueduct

All three stages complete in 416.721 / 117.979 / 240.472 seconds, with 16/8/11
started model steps and 18/5/6 MCP calls. Nominal harness cost is $0.206330436.
No domain failures remain in the observable trace. Original GPU fidelity objects,
PNG reads, saved lineage and unchanged frozen runtime are verified. One large CLI
receipt was truncated around other fields; its complete fidelity object remained.
The reader now recognizes that retained evidence. No authoring was repeated.

Independent exported geometry confirms length increases from 6 to 7.2 m, height
stays 4 m, all ten piers retain exact geometry and cross-sections, and the channel
retains its clear width, wall thickness and elevations. The second edit replaces
only the central lower bay. All 37 protected meshes, world transforms and material
bindings remain exact. The pair of pointed arches and 0.20 m center pier share one
editable assembly root and preserve the former 1.8 m outer bay width.

Whole-assembly rays through each arch find no obstruction at 57 baseline, 57
lengthened and 66 replacement-stage sample lines. Two unchanged instances placed
end-to-end leave all nine sampled longitudinal channel lines open at every stage;
the coping and bed heights match on both sides of each joint. These are explicitly
bounded clearance and interface checks, not structural, watertight or exhaustive
volume certification. Raised arch bands intentionally intersect their masonry.
The simple scalar-color stone remains visually pale and is not a texture test.

Evidence: `evidence-heldout-05-aqueduct-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review.json`, three trace reviews and original
artifacts. Heldout05 counts as reviewed completion with two edits. The next
sequential controller is running trial6, the daysailer.


## Heldout06: daysailer

All three stages complete in 659.431 / 185.453 / 145.995 seconds, with 24/9/6
started model steps and 26/8/5 MCP calls. Nominal harness cost is $0.198511968.
The baseline recovers from one explicit source syntax error; edits have no
retained domain failures. Original GPU receipts, saved lineage and PNG reads pass.

Independent exported hull rays at thirty mirrored positions show matching
bow surfaces; all nine sampled cockpit rays reach its recessed floor. Hull
beam changes from 1.434871793 to 1.721846104 m, approximately +20% within
Float32 precision, while its 3.399999976 m length is unchanged. Whole-boat
length includes the rudder and remains about 3.635 m. Twelve protected sail,
spar, rudder and independent rigging meshes retain geometry, world transforms
and authored material properties through the beam edit.

The furled-jib replacement preserves all 32 outside meshes. Its bundle and two
cuffs have one editable root. Rounded authored angle/length put both cylinder
axis endpoints 0.393 mm from the original jib attachment points; the endpoints
are approximate, not exact. The cuffs are solid overlapping cylinders, not
modeled cloth wraps or proof of a tight fastening. The extra material belongs
only to those new cuffs.

The author checks ropes against their cleats and reports plausible attachments,
but omits the fittings' support. Frozen CLI measurements find both jib cleats
107.005 mm from the original hull and 390 mm above the cockpit floor. After
widening, hull distance grows to 184.808 mm; the cleats remain unchanged. Their
minimum distance from the entire deck-trim assembly is 88.294 mm initially and
173.669 mm after widening. The mainsheet cleat is 24 mm above the aft coaming.
The tiller/post gap is 2.041 mm, matching a value disclosed by the model. Both
chainplates do meet their gunwale rails.

The other end of each jib sheet originally misses the sail by 15.272 mm. After
furling, the unchanged sheets end in empty space and are 1.210 m from the bundle.
Preserving unrelated geometry does not by itself handle the replaced part's
connections. These are selected surface measurements, not universal clearance
or mechanical-strength evidence. Original sources remain unmodified.

This extends F219's incomplete interface inspection. The existing tool reports
the omitted interfaces correctly. Shared guidance already calls for both support
ends; final consolidation should clarify intermediate mounts and the connection
points of a replaced part without a boat-specific rule or an automatic physical
validation claim.

Evidence: `evidence-heldout-06-daysailer-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review.json`, three stage geometry reviews,
attachment-chain CLI receipts and original traces. Heldout06 counts as reviewed
completion with both edits, with these unresolved quality failures.

## Heldout07: second task lamp

Three stages complete in 577.502 / 307.720 / 629.784 seconds, with 30/11/20
started model steps and 28/7/21 MCP calls. Nominal harness cost is $0.255996684.
An invented exact circle helper is rejected with suggestions. The replacement
first violates the rounded-box radius contract, then recovers from the existing
actionable diagnostic. GPU, source/GLB, PNG-read and saved-lineage checks pass.

The lower link grows exactly 0.28 to 0.35 m; the upper link remains 0.22 m. All
ten base meshes remain exact. Only three lower-link geometries change, with
attached upper assemblies translated through the hierarchy. The rectangular
head replacement preserves 29 protected meshes and material bindings, the head
pivot, and all three exported Fold channels. It has one replaceable root.

Independent exported-geometry checks find all six cap/pin gaps at 0.655 mm. The
original diffuser is 2.998 mm from its shade; the replacement diffuser meets its
new backing shell. The cable meets the head pin and its gland, but is 1.991 mm
from the housing at rest. At sampled Fold phases 0 / .13 / .37 / .61 / 1,
housing distance is 1.991 / 0.440 / 0 / 0 / 0 mm while cable/pin contact remains
zero. Thus the author's claim of housing contact at rest is unsupported, though
the pin supplies a connection. Matching animation tracks and these samples do
not certify continuous clearance, flexible-cable behavior or full joint quality.

Evidence: `evidence-heldout-07-task-lamp-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review.json`, three trace reviews, retained
comparisons and original images. Review uses actual exported GLTFLoader /
AnimationMixer playback and frozen surface measurements, without source repairs.
Heldout07 counts as reviewed completion with both edits and the stated limits.

## Heldout08: second canal gate

Three stages complete in 955.085 / 252.008 / 294.274 seconds, with 35/8/9
started model steps and 65/4/8 MCP calls. Nominal harness cost is $0.264180378.
The baseline recovers from invented exact Discovery IDs and repeated material
slot probes, including `albedo` in `materialRecipe.textureResources`. That
contract is covered by F252's separately qualified current-checkout fix;
this frozen trial keeps its original failures. Both edits have no retained
domain failures. The original GPU receipts were redirected to stage-local
JSON files. Reading and hashing those exact files resolves the evidence-reader
stop without rerunning authoring or rendering.

The opening grows from 3 to 3.6 m. Bank outer dimensions remain unchanged, but
four face components change width by approximately 52.898 mm as the hinge
notches shift. Thus this is not exact preservation of the abutment shape.
Animation channels, material properties and texture bytes remain exact. The
rail replacement preserves all 60 protected meshes, world transforms and
bindings. Its 28 new parts stay under the moving left leaf; all 26 balusters
meet the bowed rail and deck in the selected surface checks.

Independent exported-geometry measurements find the closed plank walls meet,
but the walkway decks have a 396.303 mm gap. All four hinge bracket/pin gaps
are 174 mm; touching the leaf-side gudgeon does not attach the pin to the bank.
Brace-to-wall gaps are 5 mm, although other trim may provide local connections.
At full Open, interior sample points confirm that both decks intersect bank
faces and returns, with up to 45 mm bank-interior margin. Five sampled phases
are reviewed; this is not a continuous collision sweep. These failures remain
through both edits and extend F219's incomplete interface inspection finding.

Evidence: `evidence-heldout-08-canal-gate-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review.json`, three trace reviews and
original artifacts. The reviewer reconstructs exported geometry and plays its
rotation keys for measurement; no scored source is changed. Heldout08 counts
as reviewed completion with two edits, with explicit quality failures.

## Heldout09: second folding luggage cart

Three stages complete in 539.380 / 120.531 / 292.338 seconds, with 28/10/10
started model steps and 25/5/6 MCP calls. Nominal harness cost is $0.293817300.
There are no retained domain failures. Original source, saved/exported GLB hashes,
GPU receipt input hashes, image reads and revision lineage agree. The author uses
surface checks and exported comparisons for both edits.

The wheel-center track grows exactly from 0.33 to 0.4125 m. Only the axle rod
geometry changes; the six wheel meshes translate without changing geometry.
Materials and Fold channels stay exact. The grip replacement preserves all 22
outside meshes, world transforms and material bindings. Its padded grip and two
collars have one editable root; both collar centers and orientations stay exact.
The pad changes to the existing rubber material as requested, so full material
tables are not identical even though protected bindings are unchanged.

Selected axle/frame, hinge/frame and axle/hub interfaces meet in all three
revisions. Both hinge contacts persist at Fold phases 0/.13/.37/.61/1. The deck
stays above ground and separate from the frame at those samples; each foot plug
is 1.880 mm below the unfolded deck. This does not certify load support or a
continuous collision sweep.

The grip and collars contact the frame, but contact is not a conforming sleeve.
At five measured vertical sections, the original tube extends below the grip
envelope by 2.156–28.238 mm; the replacement leaves 5.156–19.738 mm exposed.
The requested shallow padded shape is present, but the measurements do not
establish enclosing collars or full frame coverage. No automatic failure or
mechanical-strength claim is inferred from this representation choice.

Evidence: `evidence-heldout-09-luggage-cart-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review-qualified-through-stage-2.json`,
three trace reviews and original images. The qualified reviewer uses world-space
wheel pivots and both triangle sides for section intervals. An earlier reviewer
read zero-offset wrapper groups and front faces only; its preliminary measurements
are superseded, as recorded in `geometry-review-corrections.json`. No authored
source was changed. Heldout09 counts as reviewed completion with both edits and
the stated fitting limits.

## Heldout10: second flowering branch

All three stages delivered in 375.210 / 130.505 / 134.622 seconds, with
24/11/9 started model steps and 19/11/6 completed MCP calls. Nominal harness
cost is $0.106062768. Original GPU images, saved/exported GLB hashes and retained
source lineage qualify; all three original six-view sheets were independently read.

Each upper petal widens from 25.420 to 30.504 mm in local Z, a 20% increase
within Float32 precision. Local X positions (length), transforms and UVs stay
exact. Cup height increases by up to 1.398 mm with the width; the author reports
this dependency. All 50 protected meshes remain exact. The second edit replaces
only the middle leaf's six blade/vein meshes with three narrow lobes and three
veins under its retained editable root. Its attachment transform and all 50
protected meshes remain exact; materials and textures match throughout.

Selected petal/center, neighboring-petal, stalk, petiole and replacement-lobe
interfaces have zero minimum surface distance. Each new vein was additionally
sampled at 23 centerline stations against its exported triangulated blade; none
has a separation larger than its tube radius. These bounded measurements support
the requested form and contacts, not continuous conformity, physical support or
full root-area overlap. Thin sheets remain intentionally open.

The baseline recovers from two normal-validation failures. A collapsed petal
grid creates degenerate triangles and a zero normal; recomputing a scaled
sphere's already-valid normals creates zeros at two unused pole vertices.
The current exact Discovery contracts now explain these limitations and valid
alternatives; the original frozen trial is unchanged. See
[surface guidance evidence](2026-09-23-surface-guidance.md).
The final edit also encounters one OpenCode `Unknown tool` routing response;
tool search resolves it before Kiln receives the successful edit. The trace miner
now retains that plain-text routing error separately from engine failures.

Evidence: `evidence-heldout-10-flower-branch-mcp-deepseek/geometry-review.json`,
`trial-qualification.json` and all three trace reviews. Delivery plus both edits
counts as reviewed completion with the stated limits.

## Heldout11: second two-tier aqueduct

Three stages delivered in 323.047 / 141.344 / 358.109 seconds, with 18/10/11
started model steps. Original sources, saved/exported GLBs, GPU receipts and image
reads qualify. All three original six-view sheets were independently reviewed.
The restrained untextured stone reads very pale; the author discloses zero textures.

Length grows from 6 to 7.2 m while height remains 4 m. The channel slab, walls
and coping preserve their Y/Z sections, including 1.70 m clear width and 0.35 m
walls. Horizontal exported-surface sections retain all four 0.75 m lower piers
and six 0.40 m upper piers within Float32 precision. The edit also lowers lower
imposts by 0.20 m, upper imposts by 0.05 m and lower course details by 0.10/0.13 m.
The author discloses the spring-line adjustment, but its broader claim that every
part has identical Y extents is incorrect. The requested overall dimensions and
pier cross-sections hold; the extra vertical detailing changes remain visible.

The central-bay replacement preserves all 32 unrelated meshes, transforms and
material bindings. Its own replaceable mesh contains two pointed passages and a
0.30 m center pier. Splitting the old whole-arcade mesh re-triangulates the outer
bays: their original topology cannot be called identical. Their exported surfaces
agree at 372 independently sampled through-lines. Materials and textures match.

All 57/57/66 sampled arch passage rays are clear. Nine longitudinal rays through
two joined module instances find no channel obstruction in each revision; sampled
wall and floor heights match across the join. Selected tier contacts have at most
0.000093 mm separation from Float32 representation. The replacement's two side
and top contacts meet. These checks do not certify every point, watertightness or
structural engineering; doubled end-pier rhythm and pale stone appearance remain
asset-level limitations.

Discovery rejects an invented `stoneMaterial` ID and offers the valid recipe.
Two OpenCode lazy-tool routing misses recover through search. One custom capture
fails after requesting `CentralBay` instead of the exported `Mesh_CentralBay`.
The author projects away the original error, so the trace retains only `ok:false`.
An independent subject lookup reproduces the invalid name and resolves the actual
name; it does not substitute for the missing original error. No new engine defect
is established. Existing guidance already asks authors to use returned part paths.

Evidence: `evidence-heldout-11-aqueduct-mcp-deepseek/geometry-review-qualified.json`,
`trial-qualification.json` and three trace reviews. The qualified geometry review
extends the earlier review with upper-pier section checks. Reviewed completion
includes both edits, with the measurement and claim limits above.

## Heldout12: second daysailer

Three stages delivered in 991.321 / 221.349 / 714.746 seconds, using 32/13/24
started model steps. Nominal harness cost is $0.582172092. Original source,
saved/exported GLB hashes and GPU image reads qualify. All three original sheets
were independently reviewed; the blue hull, ivory cloth and furled replacement
are visible. These are untextured materials, not production sailcloth or wood maps.

Hull beam increases from 1.349501 to 1.619402 m, 20% within Float32 precision;
its 3.60 m length and 0.763 m height stay fixed. Nineteen meshes, including both
sails, mast/boom, rudder and tiller, are exact. Cockpit width and dependent trim
move with the hull. The final jib replacement preserves 81 meshes and changes
only the two retained jib sheets to meet the roll; unrelated rigging stays exact.
One `FurledJib` root owns the bundle, two straps and two buckles. The original
leading-edge endpoint locations agree with exported end-ring centers within
0.000072 mm. Selected bundle, strap, buckle, mast and stem-fitting surfaces meet;
this is not proof of continuous wrapping or a physical fastening.

Two substantial original defects remain through both edits. A perimeter point
list jumps from the bow to the stern, producing a 3.6 m diagonal gunwale across
the cockpit. Six interior probes hit that bar. The hull's vertices and bounds
are symmetric, but its custom warped-quad triangulation is not: 164 paired
surface probes expose a maximum 17.983604 mm difference near the bow. A read-only
control mirrors the triangle connectivity with corrected winding and reduces
that difference below 7e-16 m without moving vertices. This diagnoses authored
topology, not a regression in `loftProfiles` or `sweepProfile`. Shared geometry
guidance now explains the distinction; the scored asset remains unchanged.

Twenty-one selected support interfaces were checked per revision. The mainsheet
block remains 5 mm above its cockpit floor; the other selected pairs meet within
Float32 precision. An initial reviewer compared the forward chainplates with
the side-deck pieces rather than their actual foredeck neighbor. The qualified
review corrects that selection and does not report the preliminary 25 mm
wrong-neighbor measurement as a mounting defect.

The final edit spent 25 render calls, including many rejected diagnostic variants.
Its custom frame function read Z from two-coordinate endpoints, propagating NaN
into `meshGeo` positions. The worker hid the specific finite-data rejection behind
generic execution failure. The author's final correction supplies the intended
third coordinate. Current checkout now carries a typed, engine-owned Float32
mesh-data diagnostic across worker/CLI/MCP, with no coordinate substitution or
exception-text disclosure. The original failed source gets useful advice; the
author's corrected final GLB remains byte-identical through current CLI and MCP.
Fresh-model benefit remains unqualified. Two OpenCode lazy-tool routing misses
and the baseline zero-normal failure are also retained in the trace reviews.

Evidence: `evidence-heldout-12-daysailer-mcp-deepseek/` contains
`trial-qualification.json`, `geometry-review-qualified-through-stage-2.json`,
`furl-review.json` and all three trace reviews. The earlier stage-1 geometry
review is preliminary. `mesh-data-diagnostic-review/receipt.json` records the
separate current-interface repair qualification. Reviewed completion includes
both edits, with the original geometry defects and claim limits above.

## Shared guidance follow-up

Heldouts06-08 distinguish two recurring omissions from tool defects: intermediate
fittings were never checked against their mounts, and retained parts could become
unsupported when a neighboring assembly changed. The current author/QA skills
now state the first distinction; the refinement skill calls for reviewing those
retained connections and reporting conflicts with protected components. The
existing surface measurements already expose the gaps. No automatic tolerance,
category rule, extra tool or asset-specific fix was added.

Skill format/registered-copy checks and all three skill validators pass. This qualifies the
instruction change, not fresh-model improvement. The twelve held-out trials keep
their original frozen skills. Native13 had not dispatched any model call: its
preparation was refreshed with these three shared files, retaining its original
manifest and the before/after hashes in `native13-guidance-preflight-refresh.json`.
Native-only context remains outside the shared skill tree.
