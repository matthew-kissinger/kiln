# Free Muse wiring and root renderer probe

OpenCode 1.18.30 listed and successfully invoked the exact authorized route
`opencode/muse-spark-1.3-contributor-free`. The owner accepted contributor training.
A generated external workspace was launched through OpenCode's documented entry,
with a fresh XDG configuration, pure mode and an environment allowlist. The normal
OpenCode authentication store remained available; no user-wide config was changed.
This is context isolation, not an OS sandbox. No paid provider substitution occurred.

The 39.627-second trace records one workspace AGENTS.md read and exactly two
successful `kiln_workspace_kiln_discover` calls: capabilities and a natural-language
query. Four model steps report $0 cost. No asset generation or image evidence was
requested. The checked-out runtime answered from the expected installation.
Build identity was `sha256:b808039cec9b7dbdf4c69f4e0deb95039506b80706d9771af6c483b329b8c899`;
execution/cache identity was `sha256:ac5905be02c4250e26fde7222187aedd2efa3dd8a7eee828a160abd22faacddc`.
These identify different layers and are retained separately.

“curved hollow tube” found curveToMesh, sweepProfile, pipeAlongPath, bezierCurve,
torusGeo and room. Room was irrelevant; the model correctly identified that the
returned contracts did not promise a hollow pipe wall. This observed query belongs
in D18 compound-query evaluation, not an untouched holdout. Wiring success is not
quality, vision, recipe or final-campaign acceptance.

The initial workspace correctly disclosed a renderer dependency conflict. Stale
`render-service/node_modules` resolved Three 0.185.1 instead of 0.186.0. Root deps
already had exact Three 0.186.0, webgpu 0.6.1 and pngjs 7.0.0. After path-checked,
reversible relocation of the nested directory to TEMP, the unchanged service resolved
root dependencies. No release package was built or published. All 73 renderer tests
passed against this root layout.

A separate two-object CLI probe required GPU rendering. Lazy startup succeeded on
the RTX 3070 through Dawn/D3D12, generating a 732-triangle GLB and a six-view PNG.
Visual inspection confirmed distinct metallic and matte material responses. A
subsequent independent status command joined the current managed service after the
rendering CLI exited. The probe establishes local root-layout execution, not fresh
package installation, other platforms or another device.

Neutral QA accepted the intentionally separate objects, but the generic floating-part
advisory still suggested joining them. Q02 should calibrate that advice against
multi-object/environment intent; no structural defect is inferred from separation.

Private evidence is under
`C:/Users/Mattm/X/kiln-dogfood/unification-2026-09-22/`: hashed input/skills/build,
JSONL tool events, process receipt, renderer source/GLB/PNG/log and hashed receipts.
The old nested dependencies are retained at
`C:/Users/Mattm/AppData/Local/Temp/kiln-renderer-legacy-deps-20260922`.
The full main and held-out authoring campaigns remain pending.
